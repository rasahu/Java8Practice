package com.rake.stream.example;

import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;

public class ComapringByValue {
	public static void main(String[] args) {
		/*
		 * Map<String, Integer> budget = new HashMap<>(); budget.put("clothes", 120);
		 * budget.put("grocery", 150); budget.put("transportation", 100);
		 * budget.put("utility", 130); budget.put("rent", 1150);
		 * budget.put("miscellneous", 90); Map sorted = budget .entrySet() .stream()
		 * .sorted(comparingByValue()) .collect( toMap(e -> e.getKey(), e ->
		 * e.getValue(), (e1, e2) -> e2, LinkedHashMap::new));
		 * 
		 * //in this line for using comparingByValue() it is expecting
		 * Map.Entry.comparingByValue().
		 * 
		 * 
		 */}
	

}
